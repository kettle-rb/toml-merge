# frozen_string_literal: true

RSpec.describe Toml::Merge do
  it "has a version number" do
    expect(Toml::Merge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
