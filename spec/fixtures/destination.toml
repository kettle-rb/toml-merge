# Simple TOML Destination - Modified values and extra keys
title = "My Application"

[server]
host = "production.example.com"
port = 443
timeout = 60
ssl = true

[database]
name = "production_db"
host = "db.example.com"
port = 5432
pool_size = 10

[cache]
enabled = true
ttl = 3600
